# SIBUID Version Log

## v0.1 — 2026-08-28

**Status:** Initial public reference package

Published:

- canonical SIBUID expansion and definition;
- durable problem thesis;
- subject / binding / snapshot distinction;
- policy-delimited binding model;
- binding continuity vocabulary;
- drift as an uncertainty state;
- non-normative example labeling;
- current standards landscape based on primary sources;
- open-ended historical context;
- static SEO surface, canonical URLs, sitemap, robots, structured metadata, and `llms.txt`.

### Semantic rule

The canonical definition is versioned independently from time-bound landscape/history updates.
